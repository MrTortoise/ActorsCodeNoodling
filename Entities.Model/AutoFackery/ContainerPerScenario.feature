Feature: ContainerPerScenario
	In order to do config per feature
	As a lazy dev
	I want to be able to recreate the container per scenario


Scenario: Take an iterator, iterate it and confirm values start at 0

Scenario: Take an iterator, iterate it and confirm values start at 0 still

Scenario: Take an iterator, iterate it and confirm values still start at 0 still still

